[package]
name = "chro-cli"
version = "0.1.0"
edition = "2021"
description = "Chro development launcher"
license = "Apache-2.0"

[[bin]]
name = "chro"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
