[package]
name = "document"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
indexmap = { version = "2", features = ["serde"] }
regex = "1"
serde_json = "1.0"
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3"
