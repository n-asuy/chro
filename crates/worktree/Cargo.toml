[package]
name = "worktree"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
git = { path = "../git" }
once_cell = "1.20"
thiserror = "1.0"
tokio = { version = "1", features = ["rt", "sync", "macros"] }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
