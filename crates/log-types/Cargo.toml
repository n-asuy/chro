[package]
name = "log-types"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
json-patch = "1.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
similar = { version = "2", features = ["text"] }
